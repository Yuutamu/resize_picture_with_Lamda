# frozen_string_literal: true

source 'https://rubygems.org'

# Rubyのバージョン指定
ruby '3.2.5'

# AWS SDK for S3
gem 'aws-sdk-s3', '~> 1.0'

# 画像処理ライブラリ（ImageMagickのラッパー）
gem 'mini_magick', '~> 4.12'

# JSON処理（標準ライブラリだが、明示的に指定）
# gem 'json' # Ruby標準ライブラリのため不要

group :development do
  gem 'rubocop', '~> 1.60'
end
