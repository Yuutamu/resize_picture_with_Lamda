# frozen_string_literal: true

# 画像処理ロジックを分離するモジュール
# NOTE: 現在は lambda_function.rb に直接実装されているが、将来的に画像処理ロジックをここに移動

module ImageProcessor
  # 画像処理のメソッドをここに定義
  # 例: resize_image, validate_image_format など
end
